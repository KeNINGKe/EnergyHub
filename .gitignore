# 依赖
node_modules/

# 日志
*.log
npm-debug.log*

# 编辑器
.vscode/
.idea/
.DS_Store
Thumbs.db

# 临时
*.tmp
scripts/check-results.json

# 翻译缓存需要进仓库：CI 冷启动时没有缓存会把每日配额耗尽、大量标题漏翻。
# 缓存随 feeds/ 一起提交，由 fetch-feeds.yml 的「git add feeds/」自动纳入。
# feeds/translation-cache.json

# dry-run 构建输出（开发验证用，不进仓库）
feeds/dry-run/

# 会话工作日报导出（临时，不进仓库）
work-summary-*.md
work-summary-*.txt
