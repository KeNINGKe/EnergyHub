# EnergyHub V1.1 开发任务与验收清单

> 日期：2026-08-05  
> 配套 PRD：[V1.1_PRD.md](./V1.1_PRD.md)  
> 预计周期：3 周，约 10–14 个开发人日

## 1. 实施原则

- 关键路径：数据基线 → 内容处理 → 精选页/全部动态 → 验收上线。
- 继续使用现有静态站点、GitHub Actions 和 Cloudflare，不引入新后端。
- 数据校验通过前不得覆盖线上最近一次有效数据。
- 先完成内容质量，再优化展示；不能用低价值信息填满精选数量。
- 每项任务只有在代码、测试或验收证据齐备后才能勾选。

## 2. 阶段 A：基线与数据协议（第 1–2 天）

- [x] **A-01** 保存连续 7 天真实数据样本，建立可重复使用的回放测试夹具。→ `samples/daily/` + `manifest.json`，`scripts/extract-samples.mjs`（2026-07-30 ~ 08-05）
- [x] **A-02** 人工标注至少 100 条样本：相关、无关、重复、主来源和主题。→ `samples/annotations/`（112 条，**已人工复核完成**：相关 77 / 无关 35，seed 20260805）
- [x] **A-03** 固化 `daily.json` V2、`featured.json` 和人工覆盖配置的校验规则。→ `scripts/lib/schema.mjs` + `scripts/validate-data.mjs` + `tests/schema.test.mjs`
- [x] **A-04** 为旧版 `daily.json` 设计前端兼容层。→ `scripts/lib/compat.mjs` + `tests/compat.test.mjs`（V1/V2 统一 normalize）
- [x] **A-05** 固化主题、地区、来源类型和影响方向枚举。→ `data/enums.json`
- [x] **A-06** 记录当前基线：无关率、重复率、成功来源数和主题分布。→ `samples/baseline/baseline.json`（人工标注：无关率 31.3%、重复率 2.7%、成功源 84%）

交付物：固定样本、人工标注、质量基线、数据协议（`docs/DATA_PROTOCOL.md`）和校验函数。

完成条件：相同输入重复执行，得到结构一致、ID 稳定的输出。→ `npm test` 25 项通过（覆盖 ID 稳定性与样本可复现性）。

## 3. 阶段 B：采集与内容处理（第 3–6 天）

- [x] **B-01** 将强主题词、组合词、通用词和负面词拆成可维护配置。→ `data/filters.json`（强/组合/通用/负面四类，负词优先）
- [x] **B-02** 实现相关性硬过滤，并为每条被过滤内容记录原因。→ `scripts/lib/filter.mjs` + `tests/filter.test.mjs`（14 项）；112 条人工标注评估 F1 0.899（precision 0.877 / recall 0.922），`npm run eval:filter` 可复现
- [x] **B-03** 实现来源类型与一手来源识别。→ `data/source-types.json` + `scripts/lib/source.mjs` + `tests/source.test.mjs`（10 项；34 个 RSS 源全部分类）
- [x] **B-04** 实现主题、地区、实体和关键数字的基础提取。→ `data/entities.json` + `data/regions.json` + `scripts/lib/extract.mjs` + `tests/extract.test.mjs`（16 项；主题 top1 66% / top2 71%）
- [x] **B-05** 实现 URL、GUID、完全相同标题的确定性去重。→ `scripts/lib/dedup.mjs` + `tests/dedup.test.mjs`（11 项，含确定性验证）
- [x] **B-06** 实现基于时间、实体、标题和数字的相似事件合并。→ `scripts/lib/merge.mjs` + `tests/merge.test.mjs`（11 项；112 样本验证找回 Eolian BESS 重复组；语义措辞差异大的重复由 B-11 人工合并组兜底）
- [x] **B-07** 按“一手来源 > 信息完整 > 发布时间早 > 来源质量”选主条目。→ `scripts/lib/select.mjs` + `tests/select.test.mjs`（7 项，字典序向量）
- [x] **B-08** 实现内部重要性评分和单来源数量限制。→ `scripts/lib/score.mjs` + `tests/score.test.mjs`（6 项）
- [x] **B-09** 清洗 RSS 摘要并生成可解释的推荐理由候选。→ `scripts/lib/clean.mjs` + `tests/clean.test.mjs`（8 项）
- [x] **B-10** 生成今日观察和 5–10 条精选候选。→ `scripts/build-daily-v2.mjs` 的 `selectFeatured`（重要性 ≥3.0、每主题 ≤2、每来源 ≤1、最多 10 条；2026-08-01/02 样本无超门槛事件，如实输出 0 条精选，不凑数）
- [x] **B-11** 支持按日期应用人工覆盖配置。→ `data/editorial-overrides.json`（byDate）+ `scripts/lib/overrides.mjs` + `tests/overrides.test.mjs`（8 项；隐藏/强制精选/取消精选/主题/影响/文本/观察/合并组，错误只记录不破坏）
- [x] **B-12** 先生成临时文件，校验通过后再原子替换正式数据。→ `scripts/build-daily-v2.mjs` 的 `atomicWrite`（`.tmp` → `validateDailyV2/validateFeatured` → `rename`；失败删除临时文件，旧文件保持），`tests/pipeline.test.mjs` 有失败保持/成功替换两项验证
- [x] **B-13** 任务日志输出采集、过滤、合并、精选和失败统计。→ `logStats` 输出「原始 → 去重 → 过滤 → 事件（精选）」漏斗 + 覆盖错误计数

交付物：可重复运行的 `daily.json` V2 和 `featured.json` 生成流程。`npm run build:v2`（线上抓取）/ `npm run build:v2:replay`（7 天样本回放，dry-run 输出到 `feeds/dry-run/`）。

完成条件：7 天样本均能生成有效文件；任一校验失败时旧文件保持不变。（✅ 已满足：7 天回放全部通过校验；`tests/pipeline.test.mjs` 6 项 + 全量 123 项全绿）

## 4. 阶段 C：精选页（第 7–8 天）

- [ ] **C-01** 将主导航调整为精选、全部动态、周报、信源。
- [ ] **C-02** 使用 Hash 保存当前栏目，刷新后恢复，默认进入精选。
- [ ] **C-03** 展示数据日期、最近更新时间和过期提示。
- [ ] **C-04** 实现 3–5 条今日观察。
- [ ] **C-05** 实现精选事件卡片及缺失可选字段时的安全降级。
- [ ] **C-06** 实现关键数字、影响方向、地区和一手来源标签。
- [ ] **C-07** 实现相关来源折叠、展开和外链。
- [ ] **C-08** 实现无精选、数据加载失败和旧数据状态。
- [ ] **C-09** 增加“查看全部动态”入口。

交付物：完整可用的精选页。

完成条件：用户可以在一个页面理解当天重点、原因和关键事实，并顺利打开原文。

## 5. 阶段 D：全部动态（第 9–10 天）

- [ ] **D-01** 将现有日报迁移为事件级全部动态。
- [ ] **D-02** 实现标题、摘要、来源、实体和标签的关键词搜索。
- [ ] **D-03** 实现主题、地区、来源类型和日期筛选。
- [ ] **D-04** 实现筛选结果数量和一键重置。
- [ ] **D-05** 每次渲染 20 条，并实现加载更多。
- [ ] **D-06** 筛选变化后重置列表数量并回到列表顶部。
- [ ] **D-07** 实现无结果和数据为空状态。
- [ ] **D-08** 验证组合筛选按交集工作且无重复事件。

交付物：可搜索、可筛选的全部动态页。

完成条件：当前数据规模下，每次搜索或筛选响应时间小于 300ms。

## 6. 阶段 E：周报、信源与视觉适配（第 11 天）

- [ ] **E-01** 将“深研”替换为“周报”，移除演示内容。
- [ ] **E-02** 无正式周报时展示真实筹备状态，不伪造日期或文章。
- [ ] **E-03** 在信源页说明其完整覆盖用途，保持现有分类导航。
- [ ] **E-04** 完成 360px、768px 和 1280px 三档布局。
- [ ] **E-05** 统一加载、空数据、错误和过期状态样式。
- [ ] **E-06** 检查键盘操作、焦点、文本对比度和外链提示。
- [ ] **E-07** 确认远程图片失败不会阻塞正文和卡片渲染。

交付物：完整导航、异常状态和移动端适配。

## 7. 阶段 F：测试与发布（第 12–14 天）

- [ ] **F-01** 使用 7 天样本回放，统计无关率、漏收、重复率和错误合并。
- [ ] **F-02** 对本文第 8 节的验收项逐项测试并保存结果。
- [ ] **F-03** 验证部分 RSS 失败时仍能发布有效数据。
- [ ] **F-04** 验证全部采集失败时不覆盖旧数据。
- [ ] **F-05** 验证精选 ID 缺失、配置错误和字段缺失的降级行为。
- [ ] **F-06** 验证旧版数据兼容和正式数据原子写入。
- [ ] **F-07** 验证 360px、768px、1280px 布局及主要浏览器。
- [ ] **F-08** 检查 RSS HTML 清洗和外链协议限制。
- [ ] **F-09** 更新 README、数据字段和日常维护说明。
- [ ] **F-10** 上线后连续观察 7 天，记录质量指标和规则调整。

交付物：验收记录、维护文档和 V1.1 上线版本。

## 8. 功能验收清单

- [ ] **AC-01 默认页**：首次访问或 Hash 无效时默认展示精选，导航与内容一致。
- [ ] **AC-02 精选数量**：质量达标时展示 3–5 条今日观察和 5–10 条精选；不足时不凑数。
- [ ] **AC-03 字段完整**：精选都有标题、链接、摘要、推荐理由、主题、来源和发布时间。
- [ ] **AC-04 事件合并**：同一事件只显示一个主条目，其他报道进入相关来源。
- [ ] **AC-05 搜索**：匹配标题、摘要、来源、实体和标签；清空搜索后保留其他筛选。
- [ ] **AC-06 组合筛选**：主题、地区和来源类型按交集生效，结果数同步更新。
- [ ] **AC-07 加载更多**：每次增加最多 20 条，不重复已有事件。
- [ ] **AC-08 失败保护**：采集、生成或校验失败时不覆盖最近一次有效数据。
- [ ] **AC-09 过期提示**：数据超过 24 小时时显示最近更新时间，不标记为今日最新。
- [ ] **AC-10 移动端**：360px 下无页面级横向滚动，核心操作均可使用。
- [ ] **AC-11 向后兼容**：旧版数据至少能展示标题、来源、时间、摘要和链接。
- [ ] **AC-12 安全展示**：不执行 RSS 中的不可信代码，不生成非 HTTP(S) 外链。

## 9. 质量目标

| 指标 | V1.1 目标 | 统计方式 |
|---|---:|---|
| 精选明显无关率 | ≤ 5% | 每日人工抽查 |
| 精选重复率 | ≤ 10% | 精选事件人工复核 |
| 相比当前重复卡片减少 | ≥ 60% | 同一 7 天样本前后对比 |
| RSS 日均成功率 | ≥ 85% | 自动任务统计 |
| 重要来源发现延迟 | ≤ 6 小时 | 发布时间与发现时间差 |
| 精选主题覆盖 | ≥ 3 个 | 当日存在高质量内容时统计 |
| 搜索与筛选响应 | < 300ms | 当前数据规模下浏览器测量 |

## 10. 异常场景检查

| 场景 | 预期结果 |
|---|---|
| 部分 RSS 失败 | 使用成功来源继续生成，日志列出失败来源 |
| 全部 RSS 失败 | 任务失败，线上保留旧数据 |
| `featured.json` 失败 | 全部动态可用，精选显示明确状态 |
| 精选 ID 不存在 | 校验失败，不发布错误精选 |
| 新字段缺失 | 隐藏对应元素，不显示空标签或 `undefined` |
| 数据超过 24 小时 | 展示过期提示和准确更新时间 |
| 翻译失败 | 展示原始标题，保留原文链接 |
| 人工覆盖错误 | 忽略错误条目并记录日志，不破坏自动结果 |

## 11. 上线检查表

### 数据与内容

- [ ] 数据日期、生成时间和统计字段正确。
- [ ] 精选 ID 全部存在于全部动态。
- [ ] 无重复 ID、非法 URL 和无效枚举。
- [ ] 精选必填字段完整。
- [ ] 推荐理由和关键数字可以由原文验证。
- [ ] 一手来源标识准确，没有错误合并不同项目。

### 前端与自动化

- [ ] 四个导航均可打开，刷新后保持当前栏目。
- [ ] 搜索、组合筛选、重置和加载更多可用。
- [ ] 相关来源可展开并正确跳转。
- [ ] 加载失败、空数据、过期和旧数据均有合理表现。
- [ ] 三档屏幕布局通过检查。
- [ ] 定时任务能生成并校验两份数据。
- [ ] 失败时不覆盖有效数据，日志可定位失败阶段。

## 12. 开发完成定义

V1.1 只有同时满足以下条件才算完成：

1. PRD 中所有 P0 用户故事可用。
2. 12 项功能验收全部通过，或已记录并明确接受例外。
3. 使用 7 天真实数据完成回放，质量指标达到目标。
4. 自动任务失败不会破坏线上已有数据。
5. 桌面端和移动端完成视觉与交互检查。
6. README、数据协议和维护方法与实际实现一致。
7. 上线后连续运行至少 3 次自动更新，无阻断错误。
